imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }

framework:
    #esi:             ~
    #translator:      { fallbacks: ["%locale%"] }
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    templating:
        engines: ['twig']
        #assets_version: SomeVersionScheme
    default_locale:  "%locale%"
    trusted_hosts:   ~
    trusted_proxies: ~
    session:
        # handler_id set to null will use default session handler from php.ini
        handler_id:  ~
    fragments:       ~
    http_method_override: true

# Twig Configuration
twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"

# Assetic Configuration
assetic:
    debug:          "%kernel.debug%"
    use_controller: false
    bundles:        [ ]
    #java: /usr/bin/java
    filters:
        cssrewrite: ~
        #closure:
        #    jar: "%kernel.root_dir%/Resources/java/compiler.jar"
        #yui_css:
        #    jar: "%kernel.root_dir%/Resources/java/yuicompressor-2.4.7.jar"

# Doctrine Configuration
doctrine:
    dbal:
        driver:   "%database_driver%"
        host:     "%database_host%"
        port:     "%database_port%"
        dbname:   "%database_name%"
        user:     "%database_user%"
        password: "%database_password%"
        charset:  UTF8

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport: "%mailer_transport%"
    host:      "%mailer_host%"
    username:  "%mailer_user%"
    password:  "%mailer_password%"
    spool:     { type: memory }

# Doctrine Migration Configuration
doctrine_migrations:
    dir_name: %kernel.root_dir%/migrations
    namespace: Application\Migrations
    table_name: migration_versions
    name: Application Migrations

# FOS Rest Bundle Configuration
fos_rest:
    param_fetcher_listener: true
    body_listener: true
    format_listener: true
    routing_loader:
        include_format: false
        default_format: json
    view:
        view_response_listener: true
        formats:
            xml: false
            json : true
        default_engine: none
    access_denied_listener:
        json: true
    allowed_methods_listener: true

# FOS Auth Configuration
fos_oauth_server:
    db_driver: orm
    client_class:        Mickadoo\Yarnyard\Bundle\ApplicationBundle\Entity\Client
    access_token_class:  Mickadoo\Yarnyard\Bundle\ApplicationBundle\Entity\AccessToken
    refresh_token_class: Mickadoo\Yarnyard\Bundle\ApplicationBundle\Entity\RefreshToken
    auth_code_class:     Mickadoo\Yarnyard\Bundle\ApplicationBundle\Entity\AuthCode
    service:
        user_provider: mickadoo_yarnyard_user.user.provider
        options:
            supported_scopes: user

# Nelmo api docs Configuration
nelmio_api_doc: ~